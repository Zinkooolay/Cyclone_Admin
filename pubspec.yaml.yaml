name: cyclone_admin
description: Cyclone AI Admin Panel
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.17.5
  firebase_core: ^2.32.0

flutter:
  uses-material-design: true